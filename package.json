{
  "name": "flutter-web-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "git clone https://github.com/flutter/flutter.git -b stable && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release"
  }
}
